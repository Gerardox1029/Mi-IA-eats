{
  "name": "mi-ia-eats-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "start": "npm start --workspace=backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
